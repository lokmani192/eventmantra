import { KeyvaluepaPipe } from './keyvaluepa.pipe';

describe('KeyvaluepaPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyvaluepaPipe();
    expect(pipe).toBeTruthy();
  });
});
