import { ResizeHeaderDirective } from './resize-header.directive';

describe('ResizeHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
